/* import { useDispatch } from "react-redux"
import { user_img } from "../store/actions/userActions" */

export default function SignIn() {
  /* const dispatch = useDispatch()
  //Simulacion usuario logueado
  const handleSignIn = () => {
    //info que supuestamente vendria de la DB
    const user = {
      img: "../../../public/user/ibadat-singh.jpg"
    }
    dispatch(user_img(user))
  } */

  return (
    <div></div>
  )
}
